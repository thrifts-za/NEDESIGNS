import graphicDesign from './graphicDesign';
import webProject from './webProject';
import client from './client';
import skill from './skill';

export const schemaTypes = [graphicDesign, webProject, client, skill];
